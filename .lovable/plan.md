

## Add Test Connect Webhook Secret

Single step: use the secrets tool to prompt you for the `STRIPE_CONNECT_TEST_WEBHOOK_SECRET` value. Once added, the webhook function will be able to verify test-mode Connect events separately from live ones.

No code changes needed — just the secret addition.

